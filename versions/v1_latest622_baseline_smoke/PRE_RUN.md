# Task22 v1 Pre-Run Record

This is a two-seed autonomous baseline on seeds `104` and `105`. Each
invocation is one episode with the public v1 entry point, official scorer
`62214036103ee8d5fef9b475dd8b344b6e2cfc03`, `MAX_STEPS=2000`, and
`REPLAN_STEPS=5`.

The run must have all `ORACLE_* = 0`, no object anchor, and no object-region,
lift, or gripper gate. It is a diagnostic baseline: no success-rate claim is
made until both raw manifests validate and the result record is pushed.
