# Task22 v15 Run Started: 2026-07-23 17:17:48 +08:00

## Intent

Run one Task22 seed-104 episode with the original `35999` VLA, its built-in
norm asset, VLM-owned prompts, and EEF-only forward timing gates.

## Preflight

- Built-in asset fingerprint verifier: passed.
- Repaired Task22 remote stage-contract verifier: passed.
- `zzhang510` read/write preflight: passed.
- One-GPU immediate probe: passed on `acd_u`.
- Two-GPU immediate probe: `acd_u` was busy; the same requested shape passed
  on `acd_ue`, which was selected for the rollout.

## Runtime Contract

- One episode, seed `104`, `MAX_STEPS=2000`, `REPLAN_STEPS=10`.
- All `ORACLE_*` controls are zero.
- No completed-task prompt injection or object-moving state reset.
- The pending result record will include the server norm-load audit, VLM prompt
  trace, EEF hold/release trace, official Task22 stage JSON, and video hashes.
