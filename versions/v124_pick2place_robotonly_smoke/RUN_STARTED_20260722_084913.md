# Task24 v124 Run Started

- Source commits: `33557ff`, `35316e7`, `67ed8f1`.
- Official scorer: RoboMemArena `62214036103ee8d5fef9b475dd8b344b6e2cfc03`.
- Slurm account: `zzhang510`.
- Seed `107`: job `428597`, node `ACD1-22`.
- Seed `108`: job `428598`, node `ACD1-6`.
- Each rollout has two GPUs: one VLA server GPU and one VLM GPU.
- Both runs passed a same-shell one-GPU allocation and private-input readability
  probe before submission.

The only experiment delta remains the robot-only `pick cookies -> place
cookies` anchor described in `PRE_RUN.md`. Outcome records must be committed
after both episodes finish.
