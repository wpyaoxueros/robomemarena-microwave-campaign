# Task24 v124 Seed107 Invalid Infrastructure Attempt

## Status

This rollout is excluded from all success-rate calculations.

- Official scorer: RoboMemArena `62214036103ee8d5fef9b475dd8b344b6e2cfc03`.
- The episode began and reached a real microwave-open EEF hold.
- The VLM autonomously emitted `pick cookies` and later `place cookies`.
- At rollout step `301`, the evaluator process terminated with
  `Aborted (core dumped)` / exit code `134`.
- `summary.tsv` contains only the header and no official episode record.

It is therefore neither a stage-only success nor a behavioral failure. The
next Task24 change must be tested from a fresh reproducible launcher rather
than treating this partial trace as an outcome.

## Immutable Evidence

Raw artifacts are retained outside this public repository. Their SHA-256
hashes are:

| Artifact | SHA-256 |
| --- | --- |
| partial `sync_vlm.log` | `a10d94de4072fdf54ab261518cb2e05a9d489c73c8a0af10fe850e42c700de0e` |
| `submit.log` | `31ee9c75ffc7acc9c1a7734b4b5767c7344d4b6ba6ee4a68f26dd6c6d13485ee` |
