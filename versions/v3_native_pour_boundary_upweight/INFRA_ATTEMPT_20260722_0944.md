# v3 Data Build Infrastructure Attempt, 2026-07-22 09:44 CST

The first v3 data-build launch did not enter Slurm or execute the builder.
`zzhang510` could read the source JSONL but could not create the new campaign
data directory because the existing shared campaign root had mode `2755`.

The owner corrected that root to `2775` while retaining group `irpn` and its
setgid bit.  No source JSONL, manifest, model, evaluator, VLA, VLM, or rollout
was created by the blocked attempt.  The next launch uses the already-pushed
v3 builder commit unchanged.
