# Task22 V3 Formal Train: Invalid Infrastructure Attempt

## Scope

- Version: `v3_native_pour_boundary_upweight`
- Dataset: accepted c4 native pour-boundary build
- Compatibility overlay: verified processor and RoPE-normalized private overlay
- Policy rule: this record contains no VLM checkpoint path.

## What Passed

The immediately preceding two-GPU, one-step probe ran on `ACD1-6` and completed:

- Slurm job: `428711`
- Per-device batch: `4`
- One optimizer step: completed
- Observed loss: `0.0163`
- Exit status: `0`

This proves that the V3 dataset, the private compatibility overlay, the OpenPI
training environment, and the selected batch can reach a finite training loss.

## Invalid Formal Attempt

The subsequent formal 500-step job is invalid and must not be treated as a VLM
result:

- Slurm job: `428717`
- Node: `ACD1-1`
- Per-device batch: `4`
- Exit status: `1`
- Training steps completed: `0`

Rank 0 failed inside DeepSpeed model preparation at `self.module.to(self.device)`:

```text
torch.OutOfMemoryError: CUDA out of memory. Tried to allocate 32.00 MiB.
GPU 0 ... 51.00 MiB is free.
Process 356419 has 31.65 GiB memory in use.
Process 349358 has 31.65 GiB memory in use.
```

The later rank-1 NCCL error followed rank-0 termination and is not the root
cause. No optimizer step, checkpoint, or VLM evaluation was produced.

## Root-Cause Status

The exact same command shape passed on `ACD1-6` but failed before the first
step on `ACD1-1`. The next attempt must first record the Slurm-visible GPU set,
the CUDA-visible mapping seen by both torchrun ranks, and pre-existing GPU
processes inside the allocation. It must reject a contaminated or incorrectly
mapped allocation before loading the full model.
