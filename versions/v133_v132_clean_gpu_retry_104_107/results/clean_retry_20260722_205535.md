# Task24 v133 Clean-GPU Retry Result

- Slurm job: `430008`
- Frozen runtime revision at launch: `164b14b`
- Official scorer: `62214036103ee8d5fef9b475dd8b344b6e2cfc03`
- Requested replacement seeds: `104` through `107`
- Prompt mode: VLM-generated prompts; all oracle prompt-injection flags were `0`.

## Result

| Category | Count |
| --- | ---: |
| Requested episodes | 4 |
| Valid official episodes | 4 |
| Invalid infrastructure episodes | 0 |
| Stage-only successes | 0 / 4 |
| Goal successes | 0 / 4 |
| Mean stage score | 33.3% |

Every valid retry completed only `01_Open_Microwave`; both required placement
stages were false. The scorer was not the failure source: its per-stage result
matches the rollout trace.

## Observed Failure Mode

After `open microwave -> pick cookies`, the VLM switched to `pick popcorn`
before it produced a stable adjacent `place cookies` prompt. The frozen adjacent
release guard kept the rollout at `pick cookies` instead of silently skipping the
cookies placement. This is a VLM prompt-transition failure, not an infrastructure
or scoring failure.

No further retry was launched after this result.
