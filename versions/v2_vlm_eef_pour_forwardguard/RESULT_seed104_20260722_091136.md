# Task22 v2 Seed104 Result

## Contract

- Source commit: `f618ff4`.
- Official scorer: RoboMemArena `62214036103ee8d5fef9b475dd8b344b6e2cfc03`.
- VLM supplied every primitive; `ORACLE_*` flags were all `0`.
- `VLM_COMPLETED_SUBTASKS_MODE=off`; no stage-prompt override or object-moving
  anchor/gate was enabled.
- v2's only behavioral addition over v1 was an EEF-only forward timing guard
  for `pick tomato` and `pour first`, plus robot-and-gripper-only release
  anchors after a VLM-selected valid next primitive.

## Result

- Seed: `104`; normal completion in 373.85 seconds.
- Official stage score: `33.3%` (`1/3`), stage-only success: `false`.
- Stage state: `Lift_Tomato_Sauce=true`, `Pour_One=false`, `Pour_Two=false`.
- The VLM correctly produced `pick tomato` and the tomato was lifted at
  rollout step 158.
- Starting at step 165, the VLM repeatedly produced `place tomato aside`, not
  `pour first`. The timing guard held the active `pick tomato` state and
  rejected the invalid forward release; it never wrote a replacement prompt.

This is a valid behavioral failure. The next hypothesis is VLM boundary-data
coverage for the visual state immediately after a successful tomato pickup,
not a scorer or VLA target-tolerance failure.

## Immutable Evidence

Raw rollout artifacts are retained outside this public repository. Their
SHA-256 hashes are:

| Artifact | SHA-256 |
| --- | --- |
| `summary.tsv` | `0decc14cc7d93cb72bbb6a66828c0dc1a7f0f44f983b85a430148c1dd89745c7` |
| `official_summary.json` | `90be0b504695a11accbc7dd7e74a7c23c5d6261c179c389a810bafda702e1607` |
| `sync_vlm.log` | `20467641409b5cc8e83898660abe1e1de960fb5f1405d1dae50959604e410d22` |
