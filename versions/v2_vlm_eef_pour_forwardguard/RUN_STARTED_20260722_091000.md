# Task22 v2 Valid Launch Record

## Smoke

- Launch token: `20260722_091000`.
- Seed: `104`.
- Episodes: `1`.
- `MAX_STEPS=2000`, `REPLAN_STEPS=5`.
- Submission uses a single remote shell: a fresh 1-GPU Slurm probe completes
  immediately before the tmux submission.

The earlier pre-probe job `428606` is explicitly excluded in
`INVALID_START_428606.md`; this is the first valid launch attempt for v2.
