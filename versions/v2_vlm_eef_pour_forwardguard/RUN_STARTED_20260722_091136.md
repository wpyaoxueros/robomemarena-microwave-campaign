# Task22 v2 Launch Record

- Launch token: `20260722_091136`.
- Seed: `104`; episodes: `1`.
- Source commit at launch: `4ce5ae5`.
- The only launch path is `dispatch_after_probe_zzhang510.sh`.
- The dispatcher runs the fresh 1-GPU probe and the 2-GPU tmux submission in
  one remote shell, with probe stdin redirected from `/dev/null`.

This is distinct from, and follows, the two no-rollout pre-probe attempts
recorded separately in this directory.
