# Task22 v17 Run Started: 2026-07-23 17:46:22 +08:00

## Purpose

One seed-104 rollout of the original `35999` VLA using its checkpoint-owned
normalization asset, the historical Task22 VLM, and the repaired remote Task22
stage scorer.

## Preflight

- Git version: `61440f0`.
- Built-in normalization asset verifier: passed.
- Pick-only EEF target verifier: passed.
- Repaired remote Task22 stage-contract verifier: passed.
- `zzhang510` read/write preflight: passed.
- One-GPU immediate probe could not allocate on `acd_u`.
- One-GPU and two-GPU immediate probes passed on `acd_ue`; that partition was
  selected for formal rollout.
- Slurm job: `432321`, user/account `zzhang510`, two GPUs.

## Contract

- All `ORACLE_*` prompt controls are zero.
- The VLM is the only producer of subtask prompts.
- No completed-task prompt injection, stage lock, object-moving anchor, object
  gate, lift gate, or gripper gate is enabled.
- Only the initial VLM-selected `pick tomato` can enter an EEF hold.  It may
  release to the VLM's actual candidate prompt; no later Task22 primitive can
  create another end-pose hold.
- Remote stage scoring requires lift, both pours, tomato placement, microwave
  open, and cookies in the microwave.  Microwave close is optional.
