# Task22 v14 Original-35999 Baseline Result

## Scope

This is the first valid current-stage rollout using the original `35999` VLA
weights. It is a behavior diagnostic, not a claimed Task22 success.

## Frozen Contract

- Task22 repaired remote stage scorer commit: `8b7710924f862ab1c8dea69adada62e8c462de40`.
- Seed `104`, one episode, `MAX_STEPS=2000`, `REPLAN_STEPS=10`.
- VLM generated every prompt. All `ORACLE_*` controls were `0`.
- Completed-subtask prompt injection, object-moving anchors, object-region
  gates, lift gates, and gripper gates were disabled.
- Required Task22 stages were lift tomato sauce, two pours, tomato aside,
  open microwave, and cookies in microwave. Microwave close remained optional.

## Result

- Stage score: `16.7%`.
- Stage-only success: `false`.
- Goal audit value: `0.1667`.
- The only completed official stage was `01_Lift_Tomato_Sauce` at rollout
  step `156`.

The VLM initially selected `pick tomato`, then began oscillating between
`place tomato aside`, `pick tomato`, `pour first`, and `pour second` before
the relevant EEF hold could be completed. The rollout therefore did not reach
either pour stage or any microwave stage.

## Diagnostic Evidence

- `pick tomato` reached the EEF target once at step `300` with distance
  `0.06468 m`, but the configured two consecutive samples were interrupted by
  an immediate VLM prompt switch.
- Minimum distances recorded by the rollout were `0.26896 m` for opening the
  microwave, `0.08979 m` for placing tomato aside, `0.30435 m` for pour one,
  and `0.31699 m` for pour two.
- Raw artifacts remain under the ignored `outputs/` directory for this version,
  including `summary.tsv`, `official_task_summary.tsv`, logs, code snapshot,
  and both videos.

## Norm Audit

The requested original norm and the checkpoint's built-in norm had the same
SHA-256 (`4f71f864b3d34e3b58616d5c01b5efa86e57b317e014a091f62f9ef13ba67a8a`).
The server nevertheless emitted a checkpoint asset fallback warning because
the external repository basename did not match the checkpoint asset ID. The
action normalization values were therefore numerically correct, but this v14
run is intentionally not treated as a clean self-contained formal reproducer.
