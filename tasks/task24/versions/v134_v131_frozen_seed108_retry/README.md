# Task24 v131 Frozen Retry

This version reruns the Task24 v131 `seed=108` configuration from its original code snapshot. Private paths are supplied through an ignored environment file. `run_one.sh` validates the fixed RoboMemArena scorer commit before it starts the episode.
