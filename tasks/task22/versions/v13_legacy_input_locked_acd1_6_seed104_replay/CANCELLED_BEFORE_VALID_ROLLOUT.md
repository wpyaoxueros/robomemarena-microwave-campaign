# v13 Cancellation Record

The historical-input-locked replay passed local and borrowed-user input
preflight, then its requested two-GPU probe on `ACD1-6` could not allocate
resources. No policy server, evaluator, episode, summary, or video started.

The user then changed the objective: stop pursuing the legacy `2999` replay
and continue Task22 only with the original `35999` VLA and matching norm.

This version has no valid rollout metric.
