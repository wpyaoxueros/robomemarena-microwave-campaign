# Task22 v16 Run Started: 2026-07-23 17:28:48 +08:00

## Purpose

One seed-104 rollout of the original `35999` VLA with built-in norm and soft
VLM-owned EEF forward gates. This is the direct one-setting follow-up to the
v15 strict-release diagnostic.

## Preflight

- Built-in norm asset verifier: passed.
- Repaired Task22 remote stage-contract verifier: passed.
- `zzhang510` read/write preflight: passed.
- One-GPU immediate probe passed on `acd_u`.
- Two-GPU immediate probe passed on `acd_ue`; that partition was selected.

## Contract

- All `ORACLE_*` controls are zero.
- No completed-task prompt injection, stage lock, object-moving anchor, object
  gate, lift gate, or gripper gate.
- The VLM remains the prompt producer. The only behavioral difference from
  v15 is that a real EEF hold may release to the VLM's current later candidate
  rather than a program-prescribed immediate label.
