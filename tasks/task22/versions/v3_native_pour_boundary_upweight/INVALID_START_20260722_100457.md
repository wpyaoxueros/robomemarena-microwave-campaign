# Invalid v3 Training Start: 2026-07-22 10:04 CST

The Slurm account probe passed.  Both one-step VLM batch probes then exited
with code `127` before model loading because the launcher invoked a bare
`torchrun` executable that is not on the borrowed account's base-shell PATH.

No training step, loss, checkpoint, or evaluation was produced.  This is an
infrastructure launch defect, not evidence about the v3 data or Task22 policy.
The corrected launcher invokes the known OpenPI Python executable through
`python -m torch.distributed.run` and is committed before retrying.
